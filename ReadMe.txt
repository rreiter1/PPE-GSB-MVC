Pour que le Projet fonctionne il faut configurer votre SQL comme ceci :

host = localhost
user = GSB_Admin  (cet utilisateur devra pourvoir create, insert et update)
password = 0550002D
database = GSBDrive

ou alors modifier le fichier config.ini qui se trouve a la racine du projet.
